import React from 'react';
import './index.less';

const WalletRight = (props) => {
  return (
    <div className="wallet-right">
      {props.children}
    </div>
  );
};
export default WalletRight;
